# ModelProxy 功能清单（v1.93 · tkinter 桌面版基线）

> 本文档是 tkinter 桌面版（v1.93，本地化运行最完整版本）的功能定稿清单，
> 作为后续 Web 管理版（v2.0 起）的范围基准：Web 版实现哪些、砍哪些、加哪些，
> 均以本清单为对照。生成于 2026-09-03。

## 一、供应商管理

| 功能 | 说明 |
|---|---|
| 供应商增删改 | 字段：ID、名称、上游地址(base_url)、API Key、并发上限、最大重试 |
| 同名自动归堆 | 列表第一列（树形列）=「名称」，同名供应商归堆显示 `名称（N）` |
| API Key 掩码 | 列表中显示前4位+****+后4位，不暴露完整密钥 |
| 改 ID 联动 | 修改供应商 ID 时自动同步修正引用它的模型（含失败后流转链） |
| 引用保护 | 删除供应商前检查模型引用 |
| 记忆 | 列宽、树形折叠状态跨启动记忆 |

## 二、模型管理

| 功能 | 说明 |
|---|---|
| 模型映射增删改 | 本地模型名 →（供应商 + 上游真实模型 ID） |
| 自定义并发 | 模型级并发上限，留空继承供应商设置 |
| 失败后流转链 | 每模型 ≤3 跳，每跳指定供应商+上游模型，按顺序流转 |
| 批量拉取 | 从供应商 `/v1/models` 拉取列表 → 多选弹窗（搜索、全选、已添加置灰、匹配计数）→ 批量编辑入库（上游名只读、本地名可改、默认 `供应商ID/上游名`、重复名校验） |
| 测试选中模型 | 直连供应商验证连通性（不经网关） |
| 纯时段模型 | 上游 ID 可留空，仅按时段路由生效 |
| 兜底模型 | default_route：未命中任何时段规则时使用的目标（可留空） |
| 树形分组 | 按供应商分组显示 `供应商（N）` |
| 交互 | 双击复制模型名；列宽/折叠记忆 |

## 三、网关设置

| 功能 | 说明 |
|---|---|
| 基本参数 | 端口、本地 Key、超时、流转等待阈值(秒)、失败后流转最大跳数 |
| 局域网监听 | 开关：绑定 0.0.0.0（开）/ 127.0.0.1（关） |
| 开机自启 | 注册表启动项 + `--autostart` 参数静默启动并自动开网关 |
| 启停 | 启动/停止按钮；运行中改配置热更新（不重启服务） |

## 四、运行监控

| 功能 | 说明 |
|---|---|
| 实时日志 | 网关请求、流转、启停事件滚动显示 |
| 统计栏 | 成功 / 重试 / 流转 / 失败 / 在途（每秒刷新） |
| 状态栏 | 运行中显示监听地址（127.0.0.1 或局域网 IP:端口） |
| Token 累计统计 | 后端按本地模型名累计 prompt/completion（config 的 token_stats 段），5 秒节流落盘；UI 不展示（v1.5 移除列） |

## 五、请求处理（网关核心）

| 功能 | 说明 |
|---|---|
| OpenAI 兼容接口 | `/v1/chat/completions`（流式 SSE + 非流式）、`/v1/models` 等 |
| 本地 Key 鉴权 | 请求须携带与设置一致的本地 Key |
| 重试退避 | 429/5xx 排队指数退避重试，受供应商最大重试数约束 |
| 流转等待阈值 | 超过阈值的持续失败触发失败后流转链 |
| 失败后流转 | 主目标失败 → 按流转链依次尝试（≤最大跳数）→ 全部失败返回错误 |
| 时段路由 | time_routes：按本机时间匹配时段自动切换上游（支持跨午夜） |
| 并发控制 | 信号量：供应商级 + 模型级并发上限 |
| Token 记录 | 从响应 usage（非流式 JSON / 流式 SSE 尾帧）提取用量并累计 |

## 六、界面与系统

| 功能 | 说明 |
|---|---|
| 窗口尺寸记忆 | 拖拽调整后自动保存（800ms 防抖），下次启动恢复；最大化状态同样记忆并恢复 |
| 托盘 | 最小化到系统托盘（关闭窗口=隐藏），托盘图标双击恢复、右键菜单退出 |
| 关于页 | 版本号 + 模型构成说明（断行在顿号后、不截断模型名） |

## 七、配置与数据

| 功能 | 说明 |
|---|---|
| config.json | 结构：gateway / providers / models / ui / token_stats |
| 数据残留清理 | 启动加载时自动清除 v0.8 时代的供应商 group 字段残留 |
| 图标资源 | tray_icon.png / Logo.png / ModelProxy.ico 打包内嵌 |

## 八、版本归档体系

| 功能 | 说明 |
|---|---|
| 版本仓库 | `D:\Projects\ModelProxy\vX.Y\`：exe + 资源 + src 快照，git 管理 |
| 版本说明 | VERSIONS.md 记录每版改动 |
| 打包 | PyInstaller（ModelGateway.spec），系统 Python 3.13 |

---

## Web 版（v2.0+）范围备注（待定项）

以下为已确认计划但**未在 v1.93 实现**的功能，将进入 Web 管理版：

1. 多 Key 分发：独立 key 生成/备注/启停、Token 与请求次数限额（日/月）、并发限制、可用模型白名单、用量监控（请求数/Token/最后使用/来源 IP）、一键禁用；key 哈希存储。
2. 公网访问支持与安全加固（IP 绑定、防爆破熔断、速率限制等）。
3. 坚果云 WebDAV 配置备份/恢复。
4. 后台服务 + Web 管理界面（替代 tkinter），后续 Docker 化迁移至 NAS。
